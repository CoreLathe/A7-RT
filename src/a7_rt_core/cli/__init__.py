# A7-RT CLI Package
"""
Command-line interface for A7-RT.

Usage:
    python -m cli init <session-path>     Create new session
    python -m cli seed <session-path>     Add nodes to session
    python -m cli run <session-path>      Run headless execution
    python -m cli config                  Manage configuration
    python -m cli stage <session-path>    Create new stage
    python -m cli narrative <session>     View session narrative
    python -m cli web                     Start web interface (stub)
"""
